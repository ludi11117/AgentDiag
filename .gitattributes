# 默认按文本处理并统一为 LF，保证跨平台与容器内脚本可执行
* text=auto eol=lf

# Windows 脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Linux 容器内脚本必须为 LF
*.sh text eol=lf
Dockerfile text eol=lf
entrypoint.sh text eol=lf
